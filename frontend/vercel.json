{
    "routes": [
      {
        "src": "/[^.]+",
        "dest": "/",
        "status": 200
      }
    ],
    "rewrites": [
      { "source": "/(.*)", "destination": "/" }
    ],
    "framework": "vite",
    "buildCommand": "npm run build",
    "outputDirectory": "dist"
  }